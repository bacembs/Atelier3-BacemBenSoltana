

{% block title %}Modifier la classe{% endblock %}

{% block body %}
    <h1>Modifier la classe "{{ form.vars.value.name }}"</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Modifier</button>
    {{ form_end(form) }}

    <a href="{{ path('classroom_affiche') }}">Retour à la liste des classes</a>
{% endblock %}